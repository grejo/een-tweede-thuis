# Netlify-configuratie voor "Een Tweede Thuis"
# Statische site — geen build nodig, gewoon de hoofdmap publiceren.

[build]
  publish = "."

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000"
